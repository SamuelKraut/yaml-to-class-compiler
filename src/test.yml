http:
  routers:
    api:
      rule: Host(`traefik.wildwolfwuff.de`)
      middlewares:
        - api-auth
      service: api@internal
      entrypoints:
        - secure
      tls:
        certresolver:
          - letsencrypt
  services:
    api:
      loadbalancer:
        server:
          port: 8080
  middlewares:
    api-auth:
      basicauth:
        users:
        - "admin"
      entrypoints:
        - secure
      tls:
        certresolver: letsencrypt
    fail2ban:
      plugin:
        fail2ban:
          logLevel: "INFO"
          bantime: "3h"
          findtime: "3m"
          maxretry: 3
          enabled: true